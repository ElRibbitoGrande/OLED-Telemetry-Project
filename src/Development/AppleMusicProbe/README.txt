Apple Music Session Probe v5

Extract so this script is located at:
C:\RainmeterDenon\Development\AppleMusicProbe\Test-AppleMusicSession.ps1

This version avoids calling Dispose() on WinRT/COM stream wrappers and verifies that exported artwork exists and is nonzero.
